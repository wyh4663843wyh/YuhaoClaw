# AI 自学习生成的好句叠加层 · 初始为空
# ------------------------------------------------------------------
# 由 reply_self_learn.py 在「样本→诊断→三路择优→打分→回灌」后写入。
# _load_strategy() 会把本文件的 lines 按同 key 并入主库(reply_strategy.yaml)。
# 保持空的默认态即可；真实业务跑起来后自动长肉。
# ------------------------------------------------------------------
meta:
  version: '1.0'
  updated: ''
  note: ''
  config:
    llm_tries: 3
strategies: {}
